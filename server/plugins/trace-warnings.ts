export default defineNitroPlugin(() => {
    // process.on('warning', (warning) => {
    //     if (warning.name === 'MaxListenersExceededWarning') {
    //         console.error('🚨 [Bun Warning Trace]');
    //         console.error(warning.stack);
    //     }
    // });
});